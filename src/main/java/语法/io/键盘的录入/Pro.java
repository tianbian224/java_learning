package 语法.io.键盘的录入;

import java.io.*;

public class Pro
{
	/*
	 * ????? 1,InputStreamReader ??????????Reader
	 * 2??InputstreamReader?????????????????????????System.in
	 * 3??????????????��??????BufferedReader BufferedReader br=new
	 * Bufferedreader(new InputSteamReader(system.in))
	 **/

	public static void main(String[] args) throws IOException
	{
		// KeyBoardToConsole();
		Utf8ToASCII();// ???
		Utf88("a.txt", "UTF-8");// ???utf-8?????????????????????��??????utf-8????
		Utf88("b.txt", "UTF-16LE");// ???UTF-16LE??????????????????��??????UTF-16LE?????????????
	}

	public static void KeyBoardToConsole() throws IOException
	{
		BufferedReader bReader = new BufferedReader(new InputStreamReader(System.in));
		String str = null;
		while ((str = bReader.readLine()) != null)
		{
			System.out.println(str.toUpperCase());
		}
	}

	/*
	 * ?????????utf-8????????
	 */
	public static void Utf88(String path, String charsetString) throws IOException
	{
		FileInputStream ii = new FileInputStream(path);
		InputStreamReader iReader = new InputStreamReader(ii, charsetString);// ??????????????????????
		char[] buffer = new char[1024]; // ????????utf-8?????
		int num = 0;
		while ((num = iReader.read(buffer)) != -1)
		{
			System.out.println(new String(buffer, 0, num));
		}
		iReader.close();
		System.out.println("??????");
	}

	/*
	 * ????????utf-8????????????US-ASCII???????????????
	 */
	public static void Utf8ToASCII() throws IOException
	{
		FileInputStream fi = new FileInputStream("a.txt");
		InputStreamReader iReader = new InputStreamReader(fi, "UTF-8");
		FileOutputStream fo = new FileOutputStream("b.txt");
		OutputStreamWriter oWriter = new OutputStreamWriter(fo, "UTF-16LE");
		char[] buffer = new char[1024];
		int num = 0;
		while ((num = iReader.read(buffer)) != -1)
		{
			// String string=new String(buffer,0,num);
			// System.out.println(string);
			oWriter.write(buffer, 0, num);
		}
		oWriter.close();
		iReader.close();
		System.out.println("??????");

	}
}
